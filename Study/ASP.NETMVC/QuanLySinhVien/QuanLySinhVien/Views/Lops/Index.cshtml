@model IEnumerable<QuanLySinhVien.Models.Lop>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLop)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Khoa.TenKhoa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLop)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Khoa.TenKhoa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Lop_ID.Trim(' ') }) |
            @Html.ActionLink("Details", "Details", new { id=item.Lop_ID.Trim(' ') }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Lop_ID.Trim(' ') })
        </td>
    </tr>
}

</table>
